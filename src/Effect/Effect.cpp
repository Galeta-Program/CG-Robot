#include "Effect.h"

/*
Effect::Effect(effectResource resources)
{
	program = resources.gs;
	computeProgram = resources.cs;
	particleSystem = resources.ps;
}

void Effect::render()
{
	program->use();
	computeProgram->use();
	particleSystem->render();
	program->unUse();
	computeProgram->unUse();
}
*/